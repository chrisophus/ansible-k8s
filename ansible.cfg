[defaults]
# Silence deprecation warnings from ansible-core or collections (e.g. to_native import).
# Remove this when you upgrade to a version that no longer triggers them.
deprecation_warnings = False
